#!/bin/python3

import os
import fnmatch
import fileinput

for line in fileinput.input("/home/nathan/Documents/iranian-hacking-info/*"):
    """
    check if spaces
    remove spaces & rename
    extract the new zip file
    rename the extraction
    delete the zip file
    """
    
for file in os.listdir('/home/nathan/Documents/iranian-hacking-info/*'):
    name_of_file = # get name of file from OS
    name_of_file.strip() # strip whitespace and leading and trailing characters
    # either rename the file or copy the old file and rename this file the new name

    if ("""file ends is * .zip"""):
        # extract the file
for file in 
